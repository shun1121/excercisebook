const songs = require( "./songs.json" );
const playdata = require( "./playdata.json" );
/*
let array = []
for(let song of songs.songs) {
    array.push(song.sort_priority)//.sort()
}
array.sort()
console.log(array)
*/
console.log(songs.songs)


